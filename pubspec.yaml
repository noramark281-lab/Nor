name: mexc_event_trader_app
description: تطبيق تداول عقود الحدث في MEXC
publish_to: 'none'
version: 1.8.0+19

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.2
  encrypt: ^5.0.1
  crypto: ^3.0.3
  provider: ^6.1.1
  intl: ^0.19.0
  fl_chart: ^0.68.0
  google_fonts: ^6.0.0
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
